{
  "name": "septena-community-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker for Septena support, feature requests, profiles, and community comments.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:migrate:local": "wrangler d1 migrations apply septena-community --local",
    "db:migrate": "wrangler d1 migrations apply septena-community"
  },
  "dependencies": {
    "@peculiar/x509": "^1.12.3",
    "cbor-x": "^1.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241101.0",
    "typescript": "^5.6.3",
    "wrangler": "^4.103.0"
  }
}
